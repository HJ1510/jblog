<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="category">	
	<insert id="firstInsert" parameterType="categoryvo"> <!-- insert 말고 다른 것을 써도 되지만 의미적으로 명시해주는 것이 좋음 -->
		<![CDATA[ 
		insert into category values(null, '기본', '카테고리를 지정하지 않은 경우', #{id })
		]]>		
	</insert>
	
	<insert id="insert" parameterType="categoryvo"> <!-- insert 말고 다른 것을 써도 되지만 의미적으로 명시해주는 것이 좋음 -->
		<![CDATA[ 
		insert into category values(null, #{title }, #{desc }, #{id })
		]]>		
	</insert>
	
	<select id="findAllById" resultType="categoryvo">
		<![CDATA[ 
		SELECT c.no , c.title , c.desc, count(p.no) as countPost 
		from category c , post p, blog b 
		where b.id =c.id and c.`no` =p.category_no and b.id = #{id }
		]]>
	</select>
</mapper>